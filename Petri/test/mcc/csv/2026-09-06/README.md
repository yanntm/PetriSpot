# Campaign of 2026-09-06: ReachabilityDeadlock rerun, and the first total examinations

Build `202609060003` (PetriSpot `--escalate` on every walk, the walk beside
the decision diagrams, streamed walker verdicts), 1800 s, 4 cores, `tall`
nodes. `REPORT.md` is generated by `../../report.py` from the CSVs here and
holds every table; this file is the reading.

## ReachabilityDeadlock (1953 instances, against `../2026-09-06-RD/`)

0 wrong. Missed 14 -> 9, bonus 11 unchanged, 38 wall hours. The three
Shield targets and five more deadlocks came in by the walk (6 to 75 s); three
FALSE proofs were lost to the wall (`DatabaseWithMutex-PT-20`, `PGCD-D02N100`
PT and COL): the escalated deadlock walk now takes 30 s per call, 4 to 6 calls
per run, ahead of the SMT proof of absence that used to land. Of the 9 missed,
5 rest on a single tool. Against the contest, we answer 1895 values to
ITS-Tools 2026's 1862 with half the time.

## The total examinations (1681 P/T instances each)

| | QuasiLivenessAll | StableMarkingAll | UpperBoundsAll |
| --- | ---: | ---: | ---: |
| atoms | 19.9 M | 4.8 M | 4.8 M |
| answered | 51 % | 89 % | 54 % |
| runs complete | 1392 | 1566 | 1278 |
| runs at the wall | 241 | 89 | 402 |
| wall hours | 189 | 117 | 260 |

* **Witnessed against proved**: QLA 9.14 M witnessed, 1.05 M proved; SMA
  3.82 M witnessed, 0.43 M proved. The walk closes 83 % of what QLA settles
  and 89 % of SMA.
* **Knee, not slope.** Complete runs close their last atom at a median 2 to
  3 s. Among the runs at the wall that reached three quarters, that point
  came at a median 4 to 13 s; the last quarter never comes.
* **QLA's residue is a few large families**: StigmergyCommit, ResIsolation,
  RERS17pb114/115, RERS2020, the three ErlangenMainframe variants hold 6.7 M of
  the 9.7 M unanswered atoms. ErlangenMainframeV2-PT-bP10C08 closes 321 of
  79 094 transitions: three walk calls of 30 to 57 s answered 375, then one
  SMT call of 687 s answered none of 78 832.
* **UBA: 313 of the 402 runs at the wall never called the walker.** Their
  last line is the structural bounds after invariants: the bounds solver
  stalls between the invariants and the walk on nets of a few thousand
  places (ASLink, SupplyChain 26 004 places, JoinFreeModules, ViralEpidemic).
  0 decision diagram verdicts in the whole examination.
* **Soundness**: `totalcheck.csv`, the vectors against the QuasiLiveness and
  StableMarking consensus: 1484 QLA and 1603 SMA instances confirmed, 0
  contradictions. `ubacheck.csv`, the UBA tokens against the 22 933 single
  place UpperBounds formulas the consensus knows: all confirmed, 0
  contradictions, 1814 undecided by the vector.
* Log volume: QLA 2.2 GB, UBA 1.8 GB, SMA 0.96 GB for 5043 runs.

The per run vectors are in `/data/ythierry/MCC26run/2026-09-06/oracles/`, the
merge into the published skeletons in `oracles-merged/` next to it.
