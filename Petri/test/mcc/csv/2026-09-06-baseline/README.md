# Baseline of 2026-09-06: the classic examinations on build 202609060003

The second submission of 2026-09-06 (RC, RF, LTLC, LTLF, CTLC, CTLF, 1953
instances each, 1800 s, 4 cores). Collected so far: **ReachabilityCardinality
only**; the others were still queued when this was written. `REPORT.md` is
generated by `../../report.py`.

## ReachabilityCardinality

**2 wrong verdicts**, the first in any campaign: `NeoElection-PT-3` formula 00
and `NeoElection-PT-7` formula 05, both `FALSE` against a four tool `TRUE`,
both tagged `TOPOLOGICAL TOPOLOGICAL TRIVIAL`. Reproduced locally in 40 s
(`Petri/test/logs/neo3-RC-local.log`). The chain: the Java reduction removes
an 892 place empty syphon, the predicate handed to the walker folds to a
constant false, PetriSpot's `WalkDriver.h` prints `FORMULA prop1 FALSE
TECHNIQUES TOPOLOGICAL TRIVIAL` (no witness can exist), and the Java reader
`PetriSpotWalker.readVerdicts` takes every FORMULA line as a witness found
without reading the value: `publishWalkerVerdict` then publishes AG = FALSE.
`INTEROP.md` says "PetriSpot never proves unreachability; every verdict it
emits is a witness": the TRIVIAL line breaks that contract, and the reader
trusts it blindly. These are the only two TRIVIAL lines in all 2026-09-06
logs and in the 2026-09-05 campaign.

Otherwise: 30427 ok, 381 missed, 133 bonus, 163 wall hours, 157 runs at the
wall. Against the contest ITS-Tools (3600 s): we answer 30562 values to its
30041, but it got more right on 40 instances, a dozen of which it closed in
under three minutes while we burned 1800 s (CloudOpsManagement, ERK, FMS,
GPPP, Kanban, MAPK, SmallOperatingSystem, ZombiesAndSurvivors, Szymanski,
SemanticWebServices). 17 `eclipse_fatal` runs are Java out of memory errors
(`eclipse.csv`, from the Eclipse logs), on COL models mostly. One log is
truncated: OAR applied a 17 minute walltime to `HouseConstruction-PT-08000`.
