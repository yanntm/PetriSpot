# Campaign of 2026-09-10 — plan and state

State as of 2026-09-10 01:35, written for the morning. Script:
`submit-2026-09-10.sh` (one examination per batch, the queue drained between
batches, never more than one examination's jobs queued).

## The legs

| leg | tool | examinations | jobs | budget | measures | baseline |
|---|---|---|---|---|---|---|
| 1 | `itstools` | RC, RF | 1953 each | 1800 s | the unvalidated ITS-Tools / PetriSpot changes; wrong, missed, bonus, at wall | `csv/2026-09-06-baseline` |
| 2 | `hsc` (TAG hsc) | CTLC, CTLF, P/T only | 1681 each | 1800 s | libHSC's CTL checker alone: four shapes, hulls, budget rounds, stop machinery | oracles; ITS-Tools' CTL 202609080313 as the field |
| 3 | `hscapprox` (TAG hscapprox) | RC, RF, P/T only | 1681 each | 300 s | the over-approximation first: verdicts tagged `TOPOLOGICAL` / `K_INDUCTION` before the fixpoint; wrong must be 0 | the local sweep (libHSC `research_notes/unreach_report.md` §2.12) |

Not in this campaign: ITS-Tools `-hsc` on CTL (the plug is wired in
`ParallelWalk`, never run at scale, no driver for it yet).

## Done tonight

* libHSC pushed (28 commits, head 24c28ad); ITS-Tools pushed (3745cb0e: the
  StateSpace `-rebuildPNML` export now drops the never-marked places and
  their consumers first — the one reduction that keeps all four values for a
  reader of a plain PNML); MCC-drivers pushed (the hsc driver's `HSC_APPROX=1`,
  the `hscapprox/` tool with its `bin/`); PetriSpot pushed (this plan, the
  submit script, `warmup_check.sh`, `watch_drain.sh`).
* Local scenarios through the harness on AirplaneLD-PT-0010 with the local
  build installed in the deploy tree: `hsc` CTLC 16/16, `itstools` RC 16/16,
  `hscapprox` RC 16/16 (13 refuted on `S`, 2 by the backward search), no
  regression line. Logs: libHSC `tests/logs/campaign/local_*.log`.
* The libHSC `HSC-Linux` build of 24c28ad landed at 01:29: installed in the
  deploy tree (`hsc/install.sh`), the scenarios rerun on it (`hsc` CTLC and
  `hscapprox` RC, 16/16 each, no regression line; libHSC
  `tests/logs/campaign/ci_*.log`), `hsc/` and `hscapprox/` rsynced to the
  cluster tree at 01:31. The cluster is ready for the warmup.
* The ITS-Tools product with 3745cb0e was published at 01:37 (the patch
  compiles). Not installed: leg 1 runs on the product already deployed on the
  cluster (202609081701), and a launcher swap before a campaign is the
  mid-flight change to avoid. Install it deliberately when you want it
  (`docs/CLUSTER.md` §1, mind the native-image choice); it also carries
  whatever hsc-pn `HSC-Linux` served at its build time, which may or may
  not be 24c28ad — check before any `-hsc` run.

## Blocked: the cluster is not free

At 01:28 our account had **64 jobs running and 4250 waiting**, 1 core each,
walltime 1:34:00 — the shape of `libHSC/experiments/order/sweep_oar.sh`
(17 heuristics × 300 s + a tenth): the order-heuristics sweep, submitted
around 01:22. At 64 concurrent jobs of up to 85 minutes that queue drains
in days, not hours. Nothing was killed and nothing was submitted on top:
the rule is one examination in the queue at a time, and the whole queue
drained before a new batch. `watch_drain.sh` looks every ten minutes
(`libHSC/tests/logs/campaign/drain_before.log`).

## To decide in the morning

1. Let the sweep drain, or cancel it (your call, not mine) to start the
   campaign now.
2. Then, in order: warmup (`BK_TOOL=itstools`, `hsc`, `hscapprox` on
   AirplaneLD-PT-0010 and a handful of instances, about 30 jobs), read it
   with `warmup_check.sh`, and if clean start `submit-2026-09-10.sh`
   detached on the head.

## Collect

`collect.sh <stamp> RC RF CTLC.hsc CTLF.hsc RC.hscapprox RF.hscapprox` into
`/data/ythierry/MCC26logs/<tool>/<build>/`, `report.py` against the two
baselines, the pages (`docs/CLUSTER.md` §4–5).

## Last look before the session closed (2026-09-10, 11:00)

Queue: 2651 of our sweep jobs waiting, **0 running** since about 09:30.
Not a quota (karma 0.13, `quota_ok`): the 19 alive `tall` nodes are all
held by other users' whole-node jobs — lacour tall15–20 (three since
2026-08-17 at 3000 h walltime, three at 150 h ending 09-13/14), clusterbd
tall2, 7, 8, 10 (since July, 3000 h), riwan tall4 (1000 h), lemattre tall6
(300 h), nibert tall1, 3, 5, 9, 11, 12, 13 (24 h jobs started 09-10, the
only near-term capacity); tall14 dead. The scheduler's estimate for our
next job: 2026-09-14 15:46. Nothing submitted, nothing killed.
`watch_drain.sh` was left looking every ten minutes
(libHSC `tests/logs/campaign/drain_before.log`).
